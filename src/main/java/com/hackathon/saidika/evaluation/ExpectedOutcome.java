package com.hackathon.saidika.evaluation;

/** End-to-end outcome the baseline pipeline is expected (or observed) to produce for a scenario. */
public enum ExpectedOutcome {
    MATCHED,
    NO_PROVIDER,
    UNSUPPORTED,
    AMBIGUOUS
}
